#include <stdio.h>

void search(int*,int*,int*);
int main() {
    int num,i,a[10],key;
    printf("Enter the number of elements should array contain : ");
    scanf("%d",&num);
    printf("Enter the elements to the number\n");
    for(i=0;i<num;i++){
    scanf("%d",&a[i]);
    }
    printf("Enter elemnent need to serached : ");
    scanf("%d",&key);
    search(a,&num,&key);

    return 0;
}
void search(int*arr,int*n,int*ele){
    int i,flag=0;
    for(i=0;i<*n;i++){
        if(*arr == *ele){
            flag=1;
            printf("The element %d found at the position of  : %d",*ele,i);
            break;
        }
        arr ++;
    }
    if(flag==0){
        printf("element not found");
    }
}
